{{ 'hp-science-page.css' | asset_url | stylesheet_tag }}

{%- style -%}
  #shopify-section-{{ section.id }} {
    margin: 0 !important;
    padding: 0 !important;
  }

  .hp-sci-grid-{{ section.id }} {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    background: {{ section.settings.background_color }};
    color: {{ section.settings.text_color }};
  }

  .hp-sci-grid-{{ section.id }}__layout {
    display: grid;
    grid-template-columns: var(--hp-sci-rail-w) minmax(0, 1fr);
    gap: clamp(28px, 4vw, 56px);
    align-items: start;
  }

  .hp-sci-grid-{{ section.id }}__rail {
    display: flex;
    justify-content: center;
    padding-top: 6px;
  }

  .hp-sci-grid-{{ section.id }}__rail-text {
    writing-mode: vertical-rl;
    transform: rotate(180deg);
    text-transform: uppercase;
    color: {{ section.settings.rail_color }};
    white-space: nowrap;
    margin: 0;
  }

  .hp-sci-grid-{{ section.id }}__heading {
    line-height: 1.06;
    margin: 0 0 clamp(48px, 7vw, 88px);
  }

  .hp-sci-grid-{{ section.id }}__heading-line {
    display: block;
  }

  .hp-sci-grid-{{ section.id }}__heading-accent {
  }

  .hp-sci-grid-{{ section.id }}__cols {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: clamp(40px, 5vw, 72px) clamp(32px, 4vw, 56px);
  }

  .hp-sci-grid-{{ section.id }}__num {
    line-height: 1;
    margin: 0 0 clamp(18px, 2.5vw, 28px);
    color: inherit;
  }

  .hp-sci-grid-{{ section.id }}__col-title {
    margin: 0 0 14px;
    line-height: 1.15;
  }

  .hp-sci-grid-{{ section.id }}__col-text {
    line-height: 1.6;
    margin: 0;
    max-width: 42ch;
    color: {{ section.settings.body_color }};
  }
  {%- if section.settings.text_offset_x_desktop != 0 or section.settings.text_offset_y_desktop != 0 -%}


  .hp-sci-grid-{{ section.id }}__text-position {
    transform: translate({{ section.settings.text_offset_x_desktop | default: 0 }}px, {{ section.settings.text_offset_y_desktop | default: 0 }}px);
  }
  {%- endif -%}

  @media screen and (max-width: 989px) {
    .hp-sci-grid-{{ section.id }}__layout {
      grid-template-columns: 1fr;
    }

    .hp-sci-grid-{{ section.id }}__rail {
      justify-content: flex-start;
    }

    .hp-sci-grid-{{ section.id }}__rail-text {
      writing-mode: horizontal-tb;
      transform: none;
    }
    {%- if section.settings.text_offset_x_mobile != 0 or section.settings.text_offset_y_mobile != 0 -%}
  
    .hp-sci-grid-{{ section.id }}__text-position {
      transform: translate({{ section.settings.text_offset_x_mobile | default: 0 }}px, {{ section.settings.text_offset_y_mobile | default: 0 }}px);
    }
    {%- endif -%}
}

  @media screen and (max-width: 640px) {
    .hp-sci-grid-{{ section.id }}__cols {
      grid-template-columns: 1fr;
    }
  }

  #shopify-section-{{ section.id }} .hp-sci .hp-sci-grid-{{ section.id }}__heading {
    font-size: {{ section.settings.heading_size_desktop | default: 52 }}px !important;
  }
  @media screen and (max-width: 989px) {
    #shopify-section-{{ section.id }} .hp-sci .hp-sci-grid-{{ section.id }}__heading {
      font-size: {{ section.settings.heading_size_mobile | default: 24 }}px !important;
    }
  }
  {% render 'hp-science-section-typography-styles', section: section %}
{%- endstyle -%}

<section class="hp-sci hp-sci-grid-{{ section.id }}">
  <div class="hp-sci__inner">
    <div class="hp-sci-grid-{{ section.id }}__layout">
      {%- if section.settings.sidebar_label != blank -%}
        <div class="hp-sci-grid-{{ section.id }}__rail" aria-hidden="true">
          <p class="hp-sci-grid-{{ section.id }}__rail-text">{{ section.settings.sidebar_label }}</p>
        </div>
      {%- endif -%}

      <div class="hp-sci-grid-{{ section.id }}__main hp-sci-grid-{{ section.id }}__text-position">
        {%- if section.settings.heading_line_1 != blank or section.settings.heading_line_2 != blank -%}
          <h2 class="hp-sci-grid-{{ section.id }}__heading">
            {%- if section.settings.heading_line_1 != blank -%}
              <span class="hp-sci-grid-{{ section.id }}__heading-line">{{ section.settings.heading_line_1 }}</span>
            {%- endif -%}
            {%- if section.settings.heading_line_2 != blank or section.settings.heading_accent != blank -%}
              <span class="hp-sci-grid-{{ section.id }}__heading-line">
                {{- section.settings.heading_line_2 -}}
                {%- if section.settings.heading_accent != blank -%}
                  <span class="hp-sci-grid-{{ section.id }}__heading-accent">{{ section.settings.heading_accent }}</span>
                {%- endif -%}
              </span>
            {%- endif -%}
          </h2>
        {%- endif -%}

        <div class="hp-sci-grid-{{ section.id }}__cols">
          {%- if section.blocks.size > 0 -%}
            {%- for block in section.blocks -%}
              <article class="hp-sci-grid-{{ section.id }}__col" {{ block.shopify_attributes }}>
                {%- if block.settings.number != blank -%}
                  <p class="hp-sci-grid-{{ section.id }}__num">{{ block.settings.number }}</p>
                {%- endif -%}
                {%- if block.settings.title != blank -%}
                  <h3 class="hp-sci-grid-{{ section.id }}__col-title">{{ block.settings.title }}</h3>
                {%- endif -%}
                {%- if block.settings.text != blank -%}
                  <p class="hp-sci-grid-{{ section.id }}__col-text">{{ block.settings.text }}</p>
                {%- endif -%}
              </article>
            {%- endfor -%}
          {%- else -%}
            <article class="hp-sci-grid-{{ section.id }}__col">
              <p class="hp-sci-grid-{{ section.id }}__num">01</p>
              <h3 class="hp-sci-grid-{{ section.id }}__col-title">Cell renewal slows</h3>
              <p class="hp-sci-grid-{{ section.id }}__col-text">At 25, skin renews itself every 28 days. By 40, that cycle has stretched to 45–60 days. The same biology is there. It just needs fewer interruptions and more support to keep pace.</p>
            </article>
            <article class="hp-sci-grid-{{ section.id }}__col">
              <p class="hp-sci-grid-{{ section.id }}__num">02</p>
              <h3 class="hp-sci-grid-{{ section.id }}__col-title">Collagen production drops</h3>
              <p class="hp-sci-grid-{{ section.id }}__col-text">After 25, collagen production falls by roughly 1% per year. The structural protein that keeps skin firm isn't disappearing. Your body is producing less of it. That's a signal problem, not a structural one.</p>
            </article>
            <article class="hp-sci-grid-{{ section.id }}__col">
              <p class="hp-sci-grid-{{ section.id }}__num">03</p>
              <h3 class="hp-sci-grid-{{ section.id }}__col-title">The barrier weakens</h3>
              <p class="hp-sci-grid-{{ section.id }}__col-text">Your skin barrier is a lipid matrix: ceramides, fatty acids, cholesterol in specific ratios. When that matrix is disrupted by over-actives, the skin loses water faster than it produces it. Most actives make this worse.</p>
            </article>
            <article class="hp-sci-grid-{{ section.id }}__col">
              <p class="hp-sci-grid-{{ section.id }}__num">04</p>
              <h3 class="hp-sci-grid-{{ section.id }}__col-title">Chronic inflammation builds</h3>
              <p class="hp-sci-grid-{{ section.id }}__col-text">Low-grade, invisible inflammation is the root of almost every visible skin concern: spots, uneven tone, sensitivity, premature ageing. The industry addresses the symptoms. Longevity science addresses the source.</p>
            </article>
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Science \u2014 Info grid",
  "tag": "section",
  "class": "hp-science-section",
  "settings": [
    {
      "type": "header",
      "content": "Text position"
    },
    {
      "type": "range",
      "id": "text_offset_x_desktop",
      "label": "Horizontal offset \u2014 desktop",
      "min": -300,
      "max": 300,
      "step": 10,
      "unit": "px",
      "default": 0,
      "info": "Move text left (negative) or right (positive)"
    },
    {
      "type": "range",
      "id": "text_offset_y_desktop",
      "label": "Vertical offset \u2014 desktop",
      "min": -300,
      "max": 300,
      "step": 10,
      "unit": "px",
      "default": 0,
      "info": "Move text up (negative) or down (positive)"
    },
    {
      "type": "range",
      "id": "text_offset_x_mobile",
      "label": "Horizontal offset \u2014 mobile",
      "min": -200,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 0,
      "info": "Move text left (negative) or right (positive)"
    },
    {
      "type": "range",
      "id": "text_offset_y_mobile",
      "label": "Vertical offset \u2014 mobile",
      "min": -200,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 0,
      "info": "Move text up (negative) or down (positive)"
    },
    {
      "type": "text",
      "id": "sidebar_label",
      "label": "Sidebar label (vertical)",
      "default": "HOW SKIN AGES"
    },
    {
      "type": "text",
      "id": "heading_line_1",
      "label": "Heading line 1",
      "default": "Skin doesn't fail."
    },
    {
      "type": "text",
      "id": "heading_line_2",
      "label": "Heading line 2 (before accent)",
      "default": "The routine "
    },
    {
      "type": "text",
      "id": "heading_accent",
      "label": "Heading accent (italic)",
      "default": "does."
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Heading & titles",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "body_color",
      "label": "Body text",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "rail_color",
      "label": "Sidebar label color",
      "default": "#111111"
    },
    {
      "type": "range",
      "id": "heading_size_desktop",
      "label": "Heading size (Desktop)",
      "min": 24,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 52
    },
    {
      "type": "range",
      "id": "heading_size_mobile",
      "label": "Heading size (Mobile)",
      "min": 16,
      "max": 56,
      "step": 2,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "rail_size",
      "label": "Sidebar label size",
      "min": 10,
      "max": 16,
      "step": 1,
      "unit": "px",
      "default": 11
    },
    {
      "type": "range",
      "id": "number_size",
      "label": "Column number size",
      "min": 32,
      "max": 64,
      "step": 2,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "column_title_size",
      "label": "Column title size",
      "min": 18,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "column_text_size",
      "label": "Column text size",
      "min": 14,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 17
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 40,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 96
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 40,
      "max": 160,
      "step": 4,
      "unit": "px",
      "default": 96
    }
  ],
  "blocks": [
    {
      "type": "column",
      "name": "Column",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Number",
          "default": "01"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Column title"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Short supporting paragraph for this point."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Science \u2014 Info grid",
      "settings": {
        "sidebar_label": "HOW SKIN AGES",
        "heading_line_1": "Skin doesn't fail.",
        "heading_line_2": "The routine ",
        "heading_accent": "does."
      },
      "blocks": [
        {
          "type": "column",
          "settings": {
            "number": "01",
            "title": "Cell renewal slows",
            "text": "At 25, skin renews itself every 28 days. By 40, that cycle has stretched to 45\u201360 days. The same biology is there. It just needs fewer interruptions and more support to keep pace."
          }
        },
        {
          "type": "column",
          "settings": {
            "number": "02",
            "title": "Collagen production drops",
            "text": "After 25, collagen production falls by roughly 1% per year. The structural protein that keeps skin firm isn't disappearing. Your body is producing less of it. That's a signal problem, not a structural one."
          }
        },
        {
          "type": "column",
          "settings": {
            "number": "03",
            "title": "The barrier weakens",
            "text": "Your skin barrier is a lipid matrix: ceramides, fatty acids, cholesterol in specific ratios. When that matrix is disrupted by over-actives, the skin loses water faster than it produces it. Most actives make this worse."
          }
        },
        {
          "type": "column",
          "settings": {
            "number": "04",
            "title": "Chronic inflammation builds",
            "text": "Low-grade, invisible inflammation is the root of almost every visible skin concern: spots, uneven tone, sensitivity, premature ageing. The industry addresses the symptoms. Longevity science addresses the source."
          }
        }
      ]
    }
  ]
}
{% endschema %}
