{%- style -%}
  .faq-custom {
    background-color: #F8F9FA;
    padding: clamp(60px, 8vw, 100px) 0;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
  }

  .faq-custom__container {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 40px;
  }

  .faq-custom__header {
    text-align: center;
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 8px;
  }

  .faq-custom__heading {
    font-size: clamp(18px, 3.5vw, 22px);
    font-weight: 600;
    color: #E02020;
    margin: 0;
    padding-bottom: 6px;
    position: relative;
    letter-spacing: -0.01em;
    text-transform: capitalize;
  }

  .faq-custom__heading::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 25%;
    right: 25%;
    height: 3px;
    background-color: #E02020;
    border-radius: 2px;
  }

  .faq-custom__items {
    background: #ffffff;
    border-radius: 16px;
    border: 1px solid rgba(0, 0, 0, 0.08);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03);
    width: 100%;
    padding: 16px 40px;
    box-sizing: border-box;
  }

  @media screen and (max-width: 768px) {
    .faq-custom__items {
      padding: 12px 20px;
    }
  }

  .faq-custom__item {
    border-bottom: 1px solid #f0f0f0;
    overflow: hidden;
  }

  .faq-custom__item:last-child {
    border-bottom: none;
  }

  .faq-custom__trigger {
    width: 100%;
    background: none;
    border: none;
    padding: 24px 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    cursor: pointer;
    text-align: left;
    color: #000000;
    outline: none;
  }

  .faq-custom__question {
    font-size: clamp(15px, 1.8vw, 17px);
    font-weight: 600;
    color: #111111;
    margin: 0;
    line-height: 1.4;
    padding-right: 20px;
    letter-spacing: -0.01em;
  }

  .faq-custom__icon-wrapper {
    width: 26px;
    height: 26px;
    background-color: #000000;
    color: #ffffff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: transform 0.3s ease, background-color 0.3s ease;
  }

  .faq-custom__icon-wrapper span {
    font-size: 16px;
    font-weight: 600;
    line-height: 1;
    transform: translateY(-1px);
    transition: transform 0.3s ease;
  }

  .faq-custom__item.active .faq-custom__icon-wrapper {
    background-color: #000000;
    transform: rotate(45deg);
  }

  .faq-custom__panel {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .faq-custom__item.active .faq-custom__panel {
    max-height: 400px;
  }

  .faq-custom__answer {
    padding-bottom: 24px;
    font-size: clamp(14px, 1.6vw, 15px);
    color: #4A4A4A;
    line-height: 1.6;
    margin: 0;
  }
{%- endstyle -%}

<div class="faq-custom">
  <div class="faq-custom__container">
    <div class="faq-custom__header">
      <h2 class="faq-custom__heading">{{ section.settings.heading | default: 'Product' }}</h2>
    </div>

    <div class="faq-custom__items">
      {%- for block in section.blocks -%}
        <div class="faq-custom__item" {{ block.shopify_attributes }}>
          <button type="button" class="faq-custom__trigger" onclick="toggleFaqCustomItem(this)">
            <h3 class="faq-custom__question">{{ block.settings.question }}</h3>
            <div class="faq-custom__icon-wrapper">
              <span>&#43;</span>
            </div>
          </button>
          <div class="faq-custom__panel">
            <p class="faq-custom__answer">{{ block.settings.answer }}</p>
          </div>
        </div>
      {%- else -%}
        <!-- Fallbacks for theme editor default preview -->
        {%- assign default_questions = "What makes the BFF fan stand out from other hand fans?|What is the battery capacity, and how long does it last on different speed settings?|Is the BFF fan safe to use around children and pets?|Can I charge it with any wire/adapter?|How will I know when my BFF is fully charged?" | split: "|" -%}
        {%- assign default_answers = "Our BFF fans utilize an ultra-premium, silent, high-RPM motor engineered for dynamic cooling and ultimate reliability.|With a high-capacity 4000mAh battery, it can continuously last up to 12 hours on the low setting and 4 hours on maximum turbo speed.|Yes, completely! The housing has closely spaced premium guards to prevent any contact with the blades.|We strongly recommend using the original Type-C cable included in the box for optimal power delivery.|The built-in intelligent LED indicator turns from blinking amber to solid green when fully charged." | split: "|" -%}
        
        {%- for q in default_questions -%}
          <div class="faq-custom__item">
            <button type="button" class="faq-custom__trigger" onclick="toggleFaqCustomItem(this)">
              <h3 class="faq-custom__question">{{ q }}</h3>
              <div class="faq-custom__icon-wrapper">
                <span>&#43;</span>
              </div>
            </button>
            <div class="faq-custom__panel">
              <p class="faq-custom__answer">{{ default_answers[forloop.index0] }}</p>
            </div>
          </div>
        {%- endfor -%}
      {%- endfor -%}
    </div>
  </div>
</div>

<script>
  function toggleFaqCustomItem(btn) {
    const item = btn.parentElement;
    const isActive = item.classList.contains('active');
    
    const items = item.parentElement.querySelectorAll('.faq-custom__item');
    items.forEach(i => i.classList.remove('active'));
    
    if (!isActive) {
      item.classList.add('active');
    }
  }
</script>

{% schema %}
{
  "name": "FAQ Accordion",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Product"
    }
  ],
  "blocks": [
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "What makes the BFF fan stand out from other hand fans?"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer",
          "default": "Our BFF fans utilize an ultra-premium, silent, high-RPM motor engineered for dynamic cooling and ultimate reliability."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FAQ Accordion",
      "blocks": [
        {
          "type": "faq_item",
          "settings": {
            "question": "What makes the BFF fan stand out from other hand fans?",
            "answer": "Our BFF fans utilize an ultra-premium, silent, high-RPM motor engineered for dynamic cooling and ultimate reliability."
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "question": "What is the battery capacity, and how long does it last on different speed settings?",
            "answer": "With a high-capacity 4000mAh battery, it can continuously last up to 12 hours on the low setting and 4 hours on maximum turbo speed."
          }
        }
      ]
    }
  ]
}
{% endschema %}
