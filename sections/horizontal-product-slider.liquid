{%- style -%}
  #shopify-section-{{ section.id }} {
    background: linear-gradient(180deg, #a80d1c 0%, #c1121f 100%);
  }

  /* SECTION */
  .hp-slider-sec {
    position: relative;
    padding: 60px 0;
    overflow: hidden;
  }

  /* HEADER */
  .hp-slider-sec__header {
    max-width: 1200px;
    margin: 0 auto 30px;
    padding: 0 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .hp-slider-sec__heading {
    font-size: clamp(20px, 3.5vw, 28px);
    font-weight: 800;
    color: #fff;
    margin: 0;
  }

  .hp-slider-sec__arrows {
    display: flex;
    gap: 12px;
  }

  /* ARROWS */
  .hp-slider-sec__arrow {
    width: 42px;
    height: 42px;
    background: rgba(255,255,255,0.15);
    border-radius: 50%;
    border: none;
    color: white;
    cursor: pointer;
    backdrop-filter: blur(10px);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.3s ease, transform 0.2s ease;
    padding: 0;
    outline: none;
  }

  .hp-slider-sec__arrow:hover {
    background: rgba(255,255,255,0.25);
    transform: scale(1.05);
  }

  .hp-slider-sec__arrow svg {
    width: 18px;
    height: 18px;
    fill: currentColor;
    display: block;
  }

  /* SLIDER */
  .hp-slider-sec__slider {
    display: flex;
    gap: 16px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    padding: 10px 20px 30px;
    scrollbar-width: none;
    cursor: grab;
    user-select: none;
    -webkit-user-select: none;
    scroll-behavior: smooth;
  }

  .hp-slider-sec__slider:active {
    cursor: grabbing;
  }

  .hp-slider-sec__slider::-webkit-scrollbar {
    display: none;
  }

  /* CARD */
  .hp-slider-sec__card {
    flex: 0 0 210px;
    background: #fff;
    border-radius: 18px;
    overflow: hidden;
    scroll-snap-align: start;
    box-shadow: 0 12px 30px rgba(0,0,0,0.08);
    transition: all 0.3s ease;
    text-decoration: none;
    display: flex;
    flex-direction: column;
  }

  .hp-slider-sec__card:hover {
    transform: translateY(-6px);
    box-shadow: 0 18px 40px rgba(0,0,0,0.12);
  }

  /* IMAGE */
  .hp-slider-sec__image-wrap {
    height: 240px;
    background: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    position: relative;
  }

  .hp-slider-sec__image {
    max-width: 90%;
    max-height: 90%;
    object-fit: contain;
    transition: transform 0.5s ease;
    display: block;
  }

  .hp-slider-sec__card:hover .hp-slider-sec__image {
    transform: scale(1.05);
  }

  /* INFO */
  .hp-slider-sec__info {
    padding: 12px;
    display: flex;
    flex-direction: column;
    gap: 4px;
    background: #ffffff;
    flex-grow: 1;
  }

  .hp-slider-sec__title {
    font-size: 14px;
    font-weight: 500;
    color: #111;
    margin: 0;
    line-height: 1.3;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    height: 2.6em;
  }

  .hp-slider-sec__price {
    font-size: 13px;
    color: #666;
    font-weight: 500;
    margin-top: auto;
  }

  /* FADE FIX */
  .hp-slider-sec__fade {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 70px;
    pointer-events: none;
    z-index: 2;
    transition: opacity 0.3s ease;
  }

  .hp-slider-sec__fade--left {
    left: 0;
    background: linear-gradient(to right, #c1121f, transparent);
  }

  .hp-slider-sec__fade--right {
    right: 0;
    background: linear-gradient(to left, #c1121f, transparent);
  }

  /* MOBILE */
  @media(max-width:768px){
    .hp-slider-sec__card{
      flex: 0 0 160px;
    }

    .hp-slider-sec__image-wrap{
      height: 200px;
    }

    .hp-slider-sec__fade {
      width: 40px;
    }
  }
{%- endstyle -%}

<div class="hp-slider-sec" id="slider-{{ section.id }}">
  <div class="hp-slider-sec__container">
    {%- if section.settings.heading != blank -%}
      <div class="hp-slider-sec__header">
        <h2 class="hp-slider-sec__heading">{{ section.settings.heading }}</h2>
        <div class="hp-slider-sec__arrows">
          <button class="hp-slider-sec__arrow hp-slider-sec__arrow--prev" aria-label="Previous Products">
            <svg viewBox="0 0 24 24"><path d="M15.41 16.59L10.83 12l4.58-4.59L14 6l-6 6 6 6 1.41-1.41z"/></svg>
          </button>
          <button class="hp-slider-sec__arrow hp-slider-sec__arrow--next" aria-label="Next Products">
            <svg viewBox="0 0 24 24"><path d="M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6-1.41-1.41z"/></svg>
          </button>
        </div>
      </div>
    {%- endif -%}

    <div class="hp-slider-sec__fade hp-slider-sec__fade--left"></div>
    <div class="hp-slider-sec__fade hp-slider-sec__fade--right"></div>

    <div class="hp-slider-sec__slider" id="scroller-{{ section.id }}">
      {%- assign collection = section.settings.collection -%}
      {%- if collection != blank and collection.products.size > 0 -%}
        {%- for product in collection.products limit: section.settings.products_to_show -%}
          <a href="{{ product.url }}" class="hp-slider-sec__card">
            <div class="hp-slider-sec__image-wrap">
              {%- if product.featured_media != blank -%}
                <img src="{{ product.featured_media | image_url: width: 600 }}"
                     alt="{{ product.title | escape }}"
                     class="hp-slider-sec__image"
                     loading="lazy"
                     width="300"
                     height="375">
              {%- else -%}
                {{ 'product-1' | placeholder_svg_tag: 'hp-slider-sec__image' }}
              {%- endif -%}
            </div>
            <div class="hp-slider-sec__info">
              <h3 class="hp-slider-sec__title">{{ product.title }}</h3>
              <div class="hp-slider-sec__price">{{ product.price | money }}</div>
            </div>
          </a>
        {%- endfor -%}
      {%- else -%}
        {%- for i in (1..6) -%}
          <div class="hp-slider-sec__card">
            <div class="hp-slider-sec__image-wrap">
              {%- capture current -%}{% cycle 1, 2, 3, 4, 5, 6 %}{%- endcapture -%}
              {{ 'product-' | append: current | placeholder_svg_tag: 'hp-slider-sec__image' }}
            </div>
            <div class="hp-slider-sec__info">
              <h3 class="hp-slider-sec__title">Sample Product Title {{ i }}</h3>
              <div class="hp-slider-sec__price">{{ 4999 | money }}</div>
            </div>
          </div>
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>
</div>

<script>
  (function() {
    const scroller = document.getElementById('scroller-{{ section.id }}');
    if (!scroller) return;

    // Mouse drag scrolling functionality
    let isDown = false;
    let startX;
    let scrollLeft;

    scroller.addEventListener('mousedown', (e) => {
      isDown = true;
      startX = e.pageX - scroller.offsetLeft;
      scrollLeft = scroller.scrollLeft;
      scroller.style.scrollBehavior = 'auto';
    });

    scroller.addEventListener('mouseleave', () => {
      isDown = false;
    });

    scroller.addEventListener('mouseup', () => {
      isDown = false;
      scroller.style.scrollBehavior = 'smooth';
    });

    scroller.addEventListener('mousemove', (e) => {
      if (!isDown) return;
      e.preventDefault();
      const x = e.pageX - scroller.offsetLeft;
      const walk = (x - startX) * 1.5; // Drag speed multiplier
      scroller.scrollLeft = scrollLeft - walk;
    });

    // Arrow controls
    const prevBtn = document.querySelector('#slider-{{ section.id }} .hp-slider-sec__arrow--prev');
    const nextBtn = document.querySelector('#slider-{{ section.id }} .hp-slider-sec__arrow--next');

    if (prevBtn && nextBtn) {
      prevBtn.addEventListener('click', () => {
        scroller.style.scrollBehavior = 'smooth';
        scroller.scrollLeft -= 226; // Width of card + gap
      });

      nextBtn.addEventListener('click', () => {
        scroller.style.scrollBehavior = 'smooth';
        scroller.scrollLeft += 226;
      });
    }
  })();
</script>

{% schema %}
{
  "name": "Horizontal Product Slider",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Shop Our Premium Best"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 4,
      "max": 12,
      "step": 1,
      "default": 8,
      "label": "Products to show"
    }
  ],
  "presets": [
    {
      "name": "Horizontal Product Slider",
      "settings": {
        "heading": "Shop Our Premium Best"
      }
    }
  ]
}
{% endschema %}