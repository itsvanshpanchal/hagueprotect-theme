{%- comment -%}
  Vanrosector brand landing page (hosted on this domain).
  Assign this template to a Page with handle "vanrosector"
  so it resolves at /pages/vanrosector. Even without a Page,
  templates/404.liquid renders the same content at that URL.
  Markup lives in snippets/brand-vanrosector.liquid.
{%- endcomment -%}
{%- render 'brand-vanrosector' -%}
